# Copy to .env.local and adjust. Nothing here is required to run the site —
# only /admin uses it, and it has a working default.

# Shared password for /admin (demo-only auth — see middleware.ts).
ADMIN_PASSWORD=appelectric-admin
